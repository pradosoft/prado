{
    title: 'Compare Validator (value)'
    tab_trigger: 'val'
    scope: 'text.html.prado'
    content: '<com:TCompareValidator${1: ID="${2:val}"}
	ControlToValidate="${3:controlID}"
	DataType="${4:Integer}"
	ValueToCompare="${5:0}"
	Operator="${6:Equal}"
	ErrorMessage="${7:error}"${8:
	Display="${9:Dynamic}"}/>$0'
}
