language: php

php:
  - 5.4
  - 5.5
  - 5.6

before_script:
  - composer self-update
  - composer install --prefer-source --dev
  - export PATH=./bin/:$PATH

script:
  - phing coverage

after_script:
  - php ./bin/coveralls -v
