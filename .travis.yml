language: php

php:
  - 5.3
  - 5.4
  - 5.5

before_script:
  - composer self-update
  - composer install --prefer-source --dev
  - export PATH=./bin/:$PATH

script:
  - ./bin/phing test

after_script:
  - php -d memory_limit=1G ./bin/coveralls -v